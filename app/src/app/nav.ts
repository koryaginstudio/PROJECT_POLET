/* Разделы системы.

   Первые три группы — этапы, из которых состоит любой транспортный процесс:
   сначала планируют, потом смотрят, что получается, потом вмешиваются. Так
   устроена работа в отрасли, и так же названы группы — диспетчер ищет раздел
   по стадии своей работы, а не по тому, как у нас назван экран.

   **Планирование** — «что должно получиться». Дашборд отвечает на вопрос
   «как дела в целом» и служит входом, диспетчерская строит план движком.

   **Мониторинг и контроль** — «что происходит и что получилось». Мониторинг
   читает срез в реальном времени, по часам, а не по тому моменту, который
   подвинул диспетчер. Сравнение и статистика — тот же контроль, только
   поверх результатов: они смотрят назад, на посчитанное, а не ведут смену.

   **Управление** — «что с этим делать». Единственное место, где диспетчер
   не смотрит, а вмешивается: объявляет событие и видит, чего стоит на него
   среагировать.

   **Базы данных** стоят за этапами, отдельной группой. Раньше они жили в
   середине, внутри контроля, и группа выходила вдвое длиннее остальных: пять
   справочников, между ними тонкими чертами протиснуты мониторинг, сравнение
   и статистика. Формально это контроль, а на деле — другой вопрос: этапы
   отвечают «что с этой сменой», а базы — «что у нас есть вообще», по всем
   прогонам сразу и безотносительно того, какой день сейчас открыт. Разные
   вопросы должны стоять в разных группах, а не разделяться чертой внутри
   одной. Заодно этапы снова читаются подряд, не разорванные справочниками.

   Настройки движка вынесены вниз отдельно: это не раздел, куда ходят
   смотреть, а то, что меняют редко и осознанно. */

export type SectionId =
  | 'home'
  | 'dispatch'
  | 'monitor'
  | 'control'
  | 'compare'
  | 'stats'
  | 'db-runs'
  | 'db-orders'
  | 'db-services'
  | 'db-clients'
  | 'db-routes'
  | 'db-engineers'
  | 'engine';

export interface NavEntry {
  id: SectionId;
  label: string;
  icon: string;
  /** Ключ счётчика. Одни берутся из открытого расчёта, другие из
      справочников: у базы цифра отвечает на «сколько в ней записей», а не
      «сколько их в сегодняшнем дне». Справочники грузятся при первом заходе
      в базы, поэтому до него счётчик равен нулю и не рисуется. */
  count?:
    | 'orders'
    | 'unassigned'
    | 'routes'
    | 'engineers'
    | 'runs'
    | 'compare'
    | 'dbOrders'
    | 'dbServices'
    | 'dbEngineers'
    | 'dbClients'
    | 'dbRoutes';
  /** Ноль у счётчика ничего не значит — не показывать его вовсе. Так у
      отбора к сравнению: «Сравнение 0» читалось бы как поломка, а не как
      «пока ничего не отобрали». */
  hideZero?: boolean;
  /** Тонкая черта над пунктом. Внутри длинной группы отделяет один смысловой
      блок от другого, не заводя ещё один заголовок. */
  divider?: boolean;
}

export interface NavGroup {
  title: string;
  items: NavEntry[];
}

export const NAV: NavGroup[] = [
  {
    title: 'Планирование',
    items: [
      { id: 'home', label: 'Главная', icon: 'house' },
      { id: 'dispatch', label: 'Диспетчерская', icon: 'gauge' }
    ]
  },
  {
    title: 'Мониторинг и контроль',
    items: [
      { id: 'monitor', label: 'Мониторинг', icon: 'navigation-arrow' },
      { id: 'compare', label: 'Сравнение', icon: 'shuffle', count: 'compare', hideZero: true },
      { id: 'stats', label: 'Статистика', icon: 'chart-bar' }
    ]
  },
  {
    title: 'Управление',
    /* Единственное число: раздел про одно решение за раз, и подшапка зовёт
       его «Управление воздействием». Множественное в меню читалось как
       список чего-то — перечня воздействий в разделе нет. */
    items: [{ id: 'control', label: 'Воздействие', icon: 'lightning' }]
  },
  /* Внутри группы справочники идут от общего к частному: расчёты — это
     записи о прогонах, всё остальное — то, что в прогонах участвовало.
     Разделителей здесь не нужно: группа и так об одном.

     Порядок читается как сам день: расчёты, заявки, которые в них разложены,
     инженеры, которые их выполняют, клиенты, к которым едут, и маршруты —
     то, что из всего этого получилось. Маршрут стоит последним именно
     поэтому: он не исходные данные, а результат, и открывают его реже. */
  {
    title: 'Базы данных',
    items: [
      { id: 'db-runs', label: 'Расчёты', icon: 'stack', count: 'runs', hideZero: true },
      { id: 'db-orders', label: 'Заявки', icon: 'clipboard-list', count: 'dbOrders', hideZero: true },
      { id: 'db-services', label: 'Услуги', icon: 'wrench', count: 'dbServices', hideZero: true },
      { id: 'db-engineers', label: 'Инженеры', icon: 'users', count: 'dbEngineers', hideZero: true },
      { id: 'db-clients', label: 'Клиенты', icon: 'user', count: 'dbClients', hideZero: true },
      { id: 'db-routes', label: 'Маршруты', icon: 'path', count: 'dbRoutes', hideZero: true }
    ]
  }
];

/** Отдельный пункт внизу меню: настройки стоят особняком от разделов — это
    не то, куда ходят смотреть, а то, что меняют редко и осознанно. */
export const NAV_FOOTER: NavEntry = {
  id: 'engine',
  label: 'Настройки',
  icon: 'sliders-horizontal'
};

/** Действия нижнего хэдера для каждого раздела. */
export const SUBHEADER: Record<SectionId, { title: string; views: { value: string; label: string }[] }> = {
  /* У дашборда и мониторинга вид один: каждый — самостоятельный экран, а не
     набор вкладок над общей темой. Подшапка это понимает и переключатель
     тогда не рисует. */
  home: { title: 'Главная', views: [] },
  dispatch: {
    title: 'Диспетчерская',
    /* Обзор стоит первым и потому открывается сам: диспетчерская встречает
       картой дня, а не столбцом чисел. Первое, на что смотрят у готового
       расчёта, — куда движок развёл людей; всё остальное читают потом и по
       вопросу. «Карта» рядом с ним не лишняя и не та же самая: обзор
       показывает день целиком и итоги расчёта поверх, «Карта» разбирает его
       по маршрутам со списком в правом поле. */
    views: [
      { value: 'overview', label: 'Обзор' },
      { value: 'summary', label: 'Сводка' },
      { value: 'timeline', label: 'По времени' },
      { value: 'map', label: 'Карта' },
      { value: 'kanban', label: 'По этапам' }
    ]
  },
  /* Мониторинг отвечает на «как идёт работа прямо сейчас», и ответов у него
     два. «Обзор» — тот же приём, что в диспетчерской: город во весь экран, а
     живые числа полосой поверх него; смотрят на движение, а не на таблицу.
     «Сводка» — те же числа списком, с теми, кто опаздывает, и составом
     смены: когда нужно не увидеть, а перечислить. */
  monitor: {
    title: 'Мониторинг',
    views: [
      { value: 'overview', label: 'Обзор' },
      { value: 'summary', label: 'Сводка' }
    ]
  },
  /* Воздействия — про одно решение за раз, и вкладки здесь только мешали бы:
     событие выбирают на самом экране, а не переключателем над ним. */
  control: { title: 'Управление воздействием', views: [] },
  compare: { title: 'Сравнение расчётов', views: [] },
  /* Переключателя видов у статистики нет: экран снят под новую сборку, и
     вкладки над пустым местом обещали бы содержимое, которого нет. */
  stats: { title: 'Статистика', views: [] },
  /* Три вида есть у каждой базы: список, карточки и таблица. Это три ответа
     на один вопрос «что тут есть», и каждый хорош своим. Карточка показывает
     запись целиком и тратит на это полэкрана. Таблица кладёт в строку всё,
     что о записи известно, и уезжает вбок. Список стоит между ними: запись —
     строка во всю ширину, слева знак, по которому её узнают, справа те числа,
     ради которых в базу и заходят.

     Список первым — и значит по умолчанию — во всех базах. Довод один и
     измерен на рабочем ноутбуке в 1366×768: строка списка ростом 58
     пикселей, карточка — 529. Открыв базу списком, диспетчер видит
     одиннадцать записей сразу; открыв карточками — пятую часть одной.
     Карточки стоят рядом для того, кто хочет разглядеть, таблица — для того,
     кто хочет всё до последней колонки.

     Разрезы — по расчётам, по навыкам, по видам работ — остаются, но идут
     после трёх видов: разрез отвечает не на «что тут есть», а на «как это
     разложено», и заменять его видом нечем. Первый вид в списке и есть вид
     по умолчанию. */
  'db-orders': {
    /* «По расчётам» — за тремя видами. Раньше он открывался сам: заявка
       живёт внутри расчёта, и раскладка по расчётам под заголовками с
       итогами отвечала на «как лёг день». Но открывался он карточками, и до
       первой заявки уходил экран. Сплошной список отвечает на «что тут есть»
       за одно движение колеса; разрез по расчётам — вкладкой рядом. */
    title: 'База заявок',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' },
      { value: 'runs', label: 'По расчётам' },
      { value: 'types', label: 'По видам работ' }
    ]
  },
  'db-services': {
    /* «По навыкам» — за тремя видами. Услуга и навык — разные вещи, которые
       всё время путают, и вопрос «что из этого чем покрывается» разрез
       закрывает одним взглядом; но услуг восемнадцать, и списком они
       читаются целиком, а навык стоит значком у каждой строки. */
    title: 'База услуг',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' },
      { value: 'skills', label: 'По навыкам' }
    ]
  },
  'db-clients': {
    title: 'База клиентов',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' }
    ]
  },
  'db-routes': {
    title: 'База маршрутов',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' },
      { value: 'runs', label: 'По расчётам' },
      { value: 'duty', label: 'По состоянию' }
    ]
  },
  'db-engineers': {
    title: 'База инженеров',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' }
    ]
  },
  'db-runs': {
    /* «По состоянию» — за тремя видами. Расчёт, взятый в работу, и черновики
       к нему — это два разных списка, которые до сих пор лежали одним: по
       рабочему сегодня едут, остальные остались в истории как варианты. */
    title: 'База расчётов',
    views: [
      { value: 'list', label: 'Список' },
      { value: 'cards', label: 'Карточки' },
      { value: 'table', label: 'Таблица' },
      { value: 'duty', label: 'По состоянию' }
    ]
  },
  /* Настройки разделены по тому, чем управляют: «Движок» — чем открывается
     форма нового расчёта, «Сервис» — границы смены и пороги, по которым
     интерфейс называет число проблемой, «Данные» — откуда всё берётся и что
     наша сторона держит у себя. Граница между первой и второй проходит по
     тому, меняет настройка расчёт или только его показ. */
  engine: {
    title: 'Настройки',
    views: [
      { value: 'rules', label: 'Правила расчёта' },
      { value: 'service', label: 'Сервис' },
      { value: 'data', label: 'Данные' }
    ]
  }
};

/** Разделы баз данных: они живут над прогоном, поэтому у них своя загрузка,
    выбор прогона к ним не применяется и правая панель им не нужна. */
export const DB_SECTIONS = new Set<SectionId>([
  'db-runs',
  'db-orders',
  'db-services',
  'db-clients',
  'db-routes',
  'db-engineers'
]);

export const isDbSection = (id: SectionId) => DB_SECTIONS.has(id);

/** Прогон выбирают там, где он определяет содержимое экрана целиком —
    в диспетчерской (какой план строим) и в мониторинге (за исполнением
    какого плана следим). Сравнение и статистика смотрят на все прогоны
    сразу, поэтому переключателя у них нет: он бы обещал влияние, которого
    нет. */
export const isRunScoped = (id: SectionId) =>
  id === 'dispatch' || id === 'monitor' || id === 'control';

/** Разделы, где лента расчётов стоит в подшапке.

    Мониторинг в их число не входит, хотя расчёт у него есть. Он показывает
    не расчёт, а ход работы по нему: сколько людей на объектах прямо сейчас,
    кто опаздывает, где они едут. Ряд чужих номеров над живым днём звал
    переключиться на соседний прогон — а переключать там нечего, смотрят на
    тот план, который взяли в работу. Какой это расчёт, сказано в заголовке
    и на самой полосе чисел. */
export const showsRunTabs = (id: SectionId) => isRunScoped(id) && id !== 'monitor';

/** Разделы, которые читают все прогоны сразу: им нужны справочники, а правая
    панель — нет. Статистика тоже стоит над прогоном, но справочники ей пока
    не нужны: наполнение снято под новую сборку. */
export const isCrossRun = (id: SectionId) => id === 'compare' || id === 'stats' || isDbSection(id);


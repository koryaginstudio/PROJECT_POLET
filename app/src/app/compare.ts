/* Границы набора для сравнения.

   Меньше двух сравнивать не с чем: один расчёт — это просто расчёт, и
   разбор из него выйдет тот же, что в его собственной карточке. Больше
   четырёх — ломается сам вид: отобранные стоят в ряд, во всю ширину
   экрана, и пятая колонка сжимает карточку до того, что в ней уже не
   разобрать ни карты, ни цифр. Потолок держим на отборе, а не на запуске:
   честнее погасить кнопку в карточке, чем принять пятый расчёт и потом
   отказать. */
export const COMPARE_MIN = 2;
export const COMPARE_MAX = 4;

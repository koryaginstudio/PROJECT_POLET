/* Откуда взялось то, чего в выгрузке не было.

   Часть полей набора выгрузка не содержит вовсе, и они достроены при сборке.
   Достроены не наугад — у каждого своё основание, — но человеку, который
   видит в карточке «Автомобиль», основание ниоткуда не видно, и поле читается
   как факт из учётной системы. Это хуже, чем допущение, названное вслух: на
   защите такой вопрос задают первым.

   Поэтому рядом с каждым достроенным полем стоит значок, а объяснение лежит
   здесь. Текст один на весь интерфейс: карточка, таблица и панель обязаны
   объяснять одно и то же одними словами.

   Когда транспорт начнёт приходить из учётной системы, эти объяснения уйдут
   вместе с допущением — а до тех пор они честнее молчания. */

import { transportName } from './dictionary.ts';

/** Почему у инженера именно этот транспорт. */
export function transportWhy(transport: string | null | undefined): string | null {
  if (!transport) return null;

  if (transport === 'car') {
    return (
      'Выгрузка тип транспорта не содержит — его раздал набор. Автомобиль стоит у большинства ' +
      'бригад: на аварию и на работу с кабелем иначе не выехать, катушку и стремянку пешком ' +
      'не носят. Такой инженер берёт любые заявки — и с требованием к транспорту, и без него.'
    );
  }

  return (
    'Выгрузка тип транспорта не содержит — его раздал набор. Двоим-троим в каждой зоне вместо ' +
    `автомобиля поставлен другой транспорт (здесь — ${transportName(transport).toLowerCase()}): ` +
    'если машина у всех, ограничение ТЗ по транспорту в наборе не встречается ни разу и ' +
    'проверить его нечем. Такой инженер возьмёт только заявки без требования к транспорту.'
  );
}

/* Вид работ, из которого выведено требование к транспорту. Список тот же, что
   в сборке набора: правка в одном месте требует правки в другом. */
const CARGO_WORK: Record<string, string> = {
  Авария: 'аварийный выезд на сеть',
  'Работа с кабелем': 'катушка кабеля, стремянка и сварочный аппарат',
  'Заявка на подключение': 'оборудование и кабель на объект',
  'Заказ подключения/Дозаказ оборудования': 'оборудование и кабель на объект'
};

/** Почему заявка требует транспорт — и почему у остальных требования нет. */
export function requiredTransportWhy(
  required: string | null | undefined,
  workType: string
): string | null {
  if (!required) return null;
  const cargo = CARGO_WORK[workType];
  return (
    'Требования к транспорту в выгрузке нет — оно выведено из вида работ. ' +
    (cargo ? `«${workType}»: ${cargo} — увезти это можно только машиной. ` : '') +
    'На работах, где везти нечего, требование не ставится: ТЗ указывает тип транспорта ' +
    'только при наличии ограничения.'
  );
}

/** Почему у инженера такая смена. */
export const shiftWhy =
  'График смен выгрузка не содержит. Смена собрана по нарядам самой бригады за этот день: ' +
  'от первого окна приёма до последнего, округлённая до часа и растянутая до девяти часов, ' +
  'если вышла короче. Дежурство в два часа — это след однодневной выгрузки, а не чей-то график.';

/** Почему инженер выезжает отсюда. */
export const homeWhy =
  'Адрес офиса записан в самой выгрузке последней строкой — оттуда бригады и выезжают. Это ' +
  'единственное, что выгрузка говорит о начале дня, и он один на зону.';

/** Почему у заявки такая длительность. */
export const durationWhy =
  'Длительность работ выгрузка не содержит: у наряда есть окно приёма в два часа, но сколько ' +
  'из них занимает работа — не записано. Минуты проставлены по виду работ, по одному правилу: ' +
  'сколько времени работа занимает у человека на объекте.';

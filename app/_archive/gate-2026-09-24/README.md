# Прежний вход в разделы — копия от 24.09.2026

Снято перед переделкой верхних блоков выбора действия на экранах
«Диспетчерская» и «Сравнение расчётов».

- `DispatchGate.tsx` — две карточки «Создать расчёт» / «Открыть существующий»
  и режим `list` с полным списком истории.
- `CompareScreen.tsx` — две карточки «Собрать сравнение» / «Открыть
  сохранённое», вторая гасла при пустом архиве.
- `app.css` — весь файл целиком, ради блока `.gate*` в прежнем виде.

Копия лежит для отката оформления. Расчётной логики она не касается.

# Движок: только это и стандартная библиотека Python 3.10+.
#
# Ни веб-фреймворка, ни ORM, ни клиента к картам: HTTP-сервер собран на
# `http.server`, запросы к OSRM и геокодеру идут через `curl`. Это не
# аскеза, а требование к демонстрации — чем меньше в списке, тем меньше
# способов не запуститься на чужом ноутбуке.

numpy>=1.23

# Нужна ради `scipy.optimize.milp` — точной модели на HiGHS, которой мы
# проверяем, насколько наш план далёк от оптимума. Сам планировщик без
# неё обходится.
scipy>=1.9
